# Default local stack — see compose/README.md for all profiles.
#
#   docker compose up --build
#   docker compose -f compose/base.yml -f compose/app-backend.local-dev.yml up --build

include:
  - path: compose/base.yml
  - path: compose/app-backend.local.yml
