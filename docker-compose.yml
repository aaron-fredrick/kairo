services:
  kairo-server:
    image: aaron-fredrick/kairo-server:latest
    container_name: kairo-server-test
    environment:
      DB_BACKEND: sqlite
    volumes:
      - "D:/projects/kairo/data:/usr/src/app/data"
    ports:
      - "8000:8000"