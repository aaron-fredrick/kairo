# =============================================================================
# Kairo — Root convenience alias
# =============================================================================
# This file forwards to the full local dev stack.
# For the full set of variants see infra/compose/README.md.
#
# Usage:
#   docker compose up -d                          # full local stack (images)
#   docker compose -f docker-compose.yml up -d   # same
# =============================================================================

include:
  - path: infra/compose/docker-compose.local.yml