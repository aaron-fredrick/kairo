name: CD Tag
'on':
  push:
    tags:
    - v*
permissions:
  contents: write
  security-events: none
  packages: write
jobs:
  cd-publish:
    name: Publish GHCR Images
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
    - uses: actions/checkout@v4
    - name: Build, Tag, and Push Docker Images
      run: |
        echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

        # Build images
        docker build -f infra/docker/app-backend.local.Dockerfile --target runner -t ghcr.io/${{ github.repository }}/kairo-backend:${{ github.ref_name }} .
        docker build -f infra/docker/app-register.Dockerfile -t ghcr.io/${{ github.repository }}/kairo-register:${{ github.ref_name }} .
        docker build -f infra/docker/app-backend.local.Dockerfile --target runner -t ghcr.io/${{ github.repository }}/kairo-standalone:${{ github.ref_name }} .
        docker build -f infra/docker/app-backend.local.Dockerfile --target runner -t ghcr.io/${{ github.repository }}/kairo-local:${{ github.ref_name }} .
        docker build -f infra/docker/app-backend.local-dev.Dockerfile --target dev -t ghcr.io/${{ github.repository }}/kairo-local-dev:${{ github.ref_name }} .

        # For minio, postgresql, redis, we might just tag existing or pull and tag
        docker pull minio/minio:latest
        docker tag minio/minio:latest ghcr.io/${{ github.repository }}/kairo-minio:${{ github.ref_name }}

        docker pull postgres:latest
        docker tag postgres:latest ghcr.io/${{ github.repository }}/kairo-postgresql:${{ github.ref_name }}

        docker pull redis:latest
        docker tag redis:latest ghcr.io/${{ github.repository }}/kairo-redis:${{ github.ref_name }}

        # Push images
        docker push ghcr.io/${{ github.repository }}/kairo-backend:${{ github.ref_name }}
        docker push ghcr.io/${{ github.repository }}/kairo-register:${{ github.ref_name }}
        docker push ghcr.io/${{ github.repository }}/kairo-standalone:${{ github.ref_name }}
        docker push ghcr.io/${{ github.repository }}/kairo-local:${{ github.ref_name }}
        docker push ghcr.io/${{ github.repository }}/kairo-local-dev:${{ github.ref_name }}
        docker push ghcr.io/${{ github.repository }}/kairo-minio:${{ github.ref_name }}
        docker push ghcr.io/${{ github.repository }}/kairo-postgresql:${{ github.ref_name }}
        docker push ghcr.io/${{ github.repository }}/kairo-redis:${{ github.ref_name }}

        # Save tarballs for release artifacts
        docker save ghcr.io/${{ github.repository }}/kairo-backend:${{ github.ref_name }} | gzip > kairo-backend.tar.gz
        docker save ghcr.io/${{ github.repository }}/kairo-register:${{ github.ref_name }} | gzip > kairo-register.tar.gz
        docker save ghcr.io/${{ github.repository }}/kairo-standalone:${{ github.ref_name }} | gzip > kairo-standalone.tar.gz
        docker save ghcr.io/${{ github.repository }}/kairo-local:${{ github.ref_name }} | gzip > kairo-local.tar.gz
        docker save ghcr.io/${{ github.repository }}/kairo-local-dev:${{ github.ref_name }} | gzip > kairo-local-dev.tar.gz
    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        generate_release_notes: true
        files: |
          kairo-backend.tar.gz
          kairo-register.tar.gz
          kairo-standalone.tar.gz
          kairo-local.tar.gz
          kairo-local-dev.tar.gz
