# MinIO root password (MINIO_ROOT_PASSWORD).
# Must match s3_secret_key.txt when STORAGE_BACKEND=minio.
# Copy this file to minio_root_password.txt and replace the value below.
your_minio_root_password_here
